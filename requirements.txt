# Core data processing
pandas==2.2.2
numpy==2.0.1

# Visualization
matplotlib
seaborn

# Configuration
PyYAML~=6.0.1

# WebSocket and async
websockets==12.0
asyncio

# Database
influxdb-client==1.44.0

# Standard library packages (no need to specify versions)
# ssl, json, time, datetime, pathlib, re, sys, os, threading, collections, dataclasses, logging, warnings
